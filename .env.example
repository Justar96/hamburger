# Environment Configuration
# Local development only - DO NOT COMMIT .env

# Application Environment
NODE_ENV=development
PORT=3000

# Redis Configuration (upstash)
UPSTASH_REDIS_URL=upstash-redis-url

# Security Configuration
# USER_ID_PEPPER: Secret value used for hashing user IDs to protect privacy
# IMPORTANT: This value MUST be consistent across all deployments (dev, staging, prod)
# Generate a secure random value using: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# DO NOT change this value after initial deployment - it will invalidate all existing user data
USER_ID_PEPPER=your-secure-random-pepper-value-here

# DAILY_SEED_SECRET: Secret key used for deterministic daily seed generation via HMAC-SHA256
# CRITICAL: This value MUST be consistent across ALL deployments (dev, staging, prod)
# Changing this value will alter all word generation - historical seeds will produce different results
# Generate a secure 64-character hex string using: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# This ensures cryptographic-quality randomness for the seeding engine
DAILY_SEED_SECRET=your-secure-64-char-hex-string-here

# DEBUG_SEEDING: Enable verbose logging for seeding operations (optional)
# Set to 'true' to enable detailed debug logs for word selection algorithms
# Logs include: PRNG states, slot selections, cluster tracking, wildcard selection
# WARNING: Debug mode significantly increases log volume - use only for troubleshooting
# DEBUG_SEEDING=false

# Vertex AI Configuration (for later phases)
# Uncomment and configure when implementing AI features
# GCP_PROJECT_ID=your-project-id
# GCP_LOCATION=us-central1
# VERTEX_API_KEY=your-vertex-api-key
